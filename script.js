document.addEventListener('DOMContentLoaded', () => {
    const tryButton = document.querySelector('.try-button');
    tryButton.addEventListener('click', () => {
        alert('Demo functionality not implemented in this version.');
    });
});